describe("AboutContent", () => {
  // TODO: relies on ParseRichTextBlocks (inside Article/ArticleCOntent/ParseBlocks)
  it("renders in the document (non empty about)", () => {});

  // TODO: relies on ParseRichTextBlocks (inside Article/ArticleCOntent/ParseBlocks)
  it("renders in the document (empty about)", () => {});

  // TODO: relies on ParseRichTextBlocks (inside Article/ArticleCOntent/ParseBlocks)
  it("matches the snapshot", () => {});
});
